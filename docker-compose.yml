services:
  web:
    build: .
    restart: always
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data

  worker:
    build: .
    restart: always
    env_file: .env
    command: npm run worker
    volumes:
      - ./data:/app/data
